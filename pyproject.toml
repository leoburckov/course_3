[project]
name = "hh-vacancies"
version = "0.1.0"
description = ""
authors = [
    {name = "leoburckov",email = "burckovleo@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pytest = "^8.4.2"
pytest-mock = "^3.15.1"
flake8 = "^7.3.0"
black = "^25.1.0"
isort = "^6.0.1"
mypy = "^1.18.1"
python-dotenv = "^1.1.1"

[tool.poetry]
name = "hh-vacancies"
version = "0.1.0"
description = "Project for collecting and analyzing HH vacancies"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.28.0"
psycopg2-binary = "^2.9.3"
python-dotenv = "^0.19.0"
prettytable = "^3.0.0"



[build-system]
requires = ["poetry-core"]
build-backend = "poetry-core"
